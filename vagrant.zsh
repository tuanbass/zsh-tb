if ((!$ + commands[vagrant])); then
	return
fi

alias vgu='vagrant up'
alias vgd='vagrant halt'
alias vgdd='vagrant destroy'
alias vggs='vagrant global-status'
alias vgst='vagrant status'
alias vgssh='vagrant ssh '
alias vgrl='vagrant reload '
